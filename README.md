# mclfmt

Pretty printer/formatting for `mcl` files as used in https://github.com/purpleidea/mgmt.

Static build with: `go build -tags 'noaugeas novirt'`, or `make`.

Tests run with `make test`.

Version of mgmt is the one from the _baddev_ branch.
